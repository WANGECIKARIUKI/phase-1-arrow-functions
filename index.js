function divide(x / y) {
    return x/y;
}

const x =2000;
const y =100;
const divide = x / y;
console.log(divide(x, y));

const square = a => a** a;

let b = 2;
let multiplication = (2, 2) => 2 * 2;

const add = (c,d) => c + d;

